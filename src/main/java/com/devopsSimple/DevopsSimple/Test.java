package com.devopsSimple.DevopsSimple;

import java.util.function.BiFunction;

public class Test {

    BiFunction<Integer, Integer, Long> additionner = (val1, val2) -> (long) val1 + val2;
    // interface                                     methode apply 
    // dans cette interface il y a (apply)  qu'il faut appeler 

}
